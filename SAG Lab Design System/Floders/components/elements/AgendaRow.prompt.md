One row of the day's schedule: session, what happens, duration. Stack 4–6 inside `slide-content`.

```jsx
<AgendaRow name="الجلسة الثانية" what="بناء أول MVP بالكامل داخل Claude Code" duration="٩٠ دقيقة" state="active" />
```

Use `state="active"` for the session in progress — the presenter's focus highlight, no animation needed.