Circular presenter photo plus name / role / org lines — title and closing slides.

```jsx
<PresenterBlock name="م. عبدالله السعيد" role="مدرّب المعمل" org="ساج لاب" photo="./presenter.jpg" />
```

On a dark surface the photo gains a 2px `--brand-sky` ring automatically.