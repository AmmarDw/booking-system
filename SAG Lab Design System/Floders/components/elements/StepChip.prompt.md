Numbered circle for sequences. `active` is the only teal thing on the slide — teal means state, never structure.

```jsx
<StepChip n={3} state="active" />
```

States: upcoming (outline), active (teal + halo), done (navy, check glyph).