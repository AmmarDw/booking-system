Persistent 10-day indicator: past days navy, active day teal and wider, plus an optional within-day bar.

```jsx
<DayProgress activeDay={7} within={40} />
```

Track runs right-to-left (day 1 on the right). Usually passed to `FooterBar` rather than placed alone.