The lead mark zone for ساج لاب, in three sizes with enforced clear space (0.5em all round).

```jsx
<LogoZone size="hero" />
```

Type-set placeholder: no logo file was provided. Never place it on a busy background; never pair it with the SAG TECH monogram at the same size — that mark endorses, this one leads.